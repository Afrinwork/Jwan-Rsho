# Agent Instructions

Before changing code in this repository, read [CODEX_MASTER_PROMPT.md](CODEX_MASTER_PROMPT.md) completely.

This file is the active project rule set, not optional documentation.

Core rule:

1. Read `CODEX_MASTER_PROMPT.md`.
2. Inspect the existing files.
3. Avoid duplicate logic and parallel architectures.
4. Implement only the next small, clean step.
5. Test and verify before moving on.

If any request conflicts with `CODEX_MASTER_PROMPT.md`, pause and resolve that conflict first.
