import { FeaturePlaceholder } from "@/src/components/feedback/FeaturePlaceholder";

export function MapScreen() {
  return (
    <FeaturePlaceholder
      description="Map filters, markers, and selection tools will be added here without overloading the route."
      title="Map"
    />
  );
}
